[project]
name = "arch-svg-gen"
version = "0.1.0"
description = "Generate layered architecture SVG from YAML"
requires-python = ">=3.11"
dependencies = [
  "pyyaml>=6.0",
]

[tool.uv]
package = false
